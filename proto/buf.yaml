version: v1
name: buf.build/ttm/messenger-bot
breaking:
  use:
    - FILE
lint:
  use:
    - STANDARD
  except:
    - RPC_REQUEST_STANDARD_NAME # explicit <rpc>+Request naming
    - PACKAGE_VERSION_SUFFIX # versioned naming <service>.v1beta